# Floating trigger tests

Run `node --test tests/floating-trigger.test.cjs` from the extension root. The test exercises mobile drag, tap, options, resize clamping, and the separate desktop position with a small DOM stub.
